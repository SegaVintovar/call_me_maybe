[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.13.4",
    "llm-sdk",
    "dotenv>=0.9.9",
]

[tool.uv.sources]
llm-sdk = { path = "llm_sdk" }
